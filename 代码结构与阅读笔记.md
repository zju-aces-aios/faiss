## GpuIndexFlat

查看GpuIndexFlat都会用到哪些具体的底层功能，以供后续修改

## GpuIndexFlat类的成员变量

`GpuIndex`类继承自`Index`类，新增了三个成员变量：`std::shared_ptr<GpuResources> resources_`负责管理GPU资源；`const GpuIndexConfig config_`表示该Index构建时的一些设置；`size_t minPagedSize_`表示从CPU到GPU拷贝的大小是否需要分页分批次传递到GPU中

`GpuIndexFlat`类继承自`GpuIndex`类，新增三个成员变量：`const GpuIndexFlatConfig flatConfig_`代表构建时的一些选择；`std::unique_ptr<FlatIndex> data_`在GPU上储存和管理所有的向量数据

> - GpuResources
> - GpuIndexConfig
> - GpuIndexFlatConfig
> - FlatIndex

#### FlatIndex

在GPU上以平面的方式存储和检索数据，包含以下成员变量：
- GpuResources
- const int dim_ 
    - 向量维度
- const bool useFloat16
- MemorySpace space_
    - 这部分数据使用的空间是什么类型的，是临时的还是标准GPU显存

- idx_t num
    - 当前存储了多少个向量

- DeviceVector\<char\> rawData32_/rawData16
    - 实际的底层显存空间，分别用与float32和float16数据，底层使用char，按需转换

- DeviceTensor\<float, 2, true\> vertors
    - 显存上的实际2D数组视图，分别对应 float32 和 float16 格式

- DeviceTensor<float, 1, true> norms_
    预计算的 L2 范数（做归一化、加速距离计算时用）

支持常见的，如添加、查询、重建、残差等操作

#### DeviceVector

用来管理实际的GPU显存，类似于c++的STL，作为一个容器管理，包括以下成员变量：

- GpuMemoryReservation alloc_
  - 实际的GPU显存指针
- size_t num_
  - 当前已经储存元素数量
- size_t capacity_
  - 当前分配最大容量
- GpuResources* res_
  - 记录分配显存的GPU资源管理器
- AllocInfo allocInfo_
  - 分配显存时的详细参数

支持扩容、数据访问与拷贝、内存回收与碎片管理等操作

#### GPU Resources

GPU资源管理器，具体实现如下：

**`GpuMemoryReservation`**是一个RAII风格的对象，用来管理一段GPU临时内存的生命周期，包括一下成员变量：

- GpuResources res*
  - 内存由哪个资源管理器分配的
- int device
  - 内存在哪个GPU设备上
- cudaSteam steam
  - 这块现存在哪个CUDA steam上分配使用
- void data*
  - 实际显存指针
- size_t size
  - 分配的显存的大小

**`GpuResources`**主要用于统一管理和调度GPU相关资源的抽象基类，管理以下资源：

- CUDA Stream：用于并发任务、异步操作的调度
- cuBLAS Handles：用于高性能线性代数运算上下文
- 显存分配与管理
- Pinned Memory：CPU端页锁定内存，加速主机与GPU的数据v幻术
- 流同步、异步拷贝等操作

**`StandarGpuResourcesImpl`**是一个**GPU资源管理器的标准实现类**，负责本地管理和资源调度，成员变量包括如下：

```c++
    /// Set of currently outstanding memory allocations per device
    /// device -> (alloc request, allocated ptr)
	/// 追踪每个设备上的显存分配记录
    std::unordered_map<int, std::unordered_map<void*, AllocRequest>> allocs_;

    /// Temporary memory provider, per each device
	/// 每个device一个临时显存池，用于高效小块分配内存
    std::unordered_map<int, std::unique_ptr<StackDeviceMemory>> tempMemory_;

    /// Our default stream that work is ordered on, one per each device
	/// 每个设备的主流
    std::unordered_map<int, cudaStream_t> defaultStreams_;

    /// This contains particular streams as set by the user for
    /// ordering, if any
	/// 用户设置的流
    std::unordered_map<int, cudaStream_t> userDefaultStreams_;

    /// Other streams we can use, per each device
	/// 备用流
    std::unordered_map<int, std::vector<cudaStream_t>> alternateStreams_;

    /// Async copy stream to use for GPU <-> CPU pinned memory copies
	/// 转供 CPU <--> GPU 之间传输数据的流
    std::unordered_map<int, cudaStream_t> asyncCopyStreams_;

    /// cuBLAS handle for each device
	/// 每个设备的cuBLAS handle句柄
    std::unordered_map<int, cublasHandle_t> blasHandles_;

#if defined USE_NVIDIA_CUVS
    /// raft handle for each device
    std::unordered_map<int, raft::device_resources> raftHandles_;

    /**
     * FIXME: Integrating these in a separate code path for now. Ultimately,
     * it would be nice if we use a simple memory resource abstraction
     * in Faiss so we could plug in whether to use RMM's memory resources
     * or the default.
     *
     * There's enough duplicated logic that it doesn't *seem* to make sense
     * to create a subclass only for the RMM memory resources.
     */

    // managed_memory_resource
    std::unique_ptr<rmm::mr::device_memory_resource> mmr_;

    // pinned_memory_resource
    std::unique_ptr<rmm::mr::host_memory_resource> pmr_;
#endif

    /// Pinned memory allocation for use with this GPU
	/// 页锁定内存指针
    void* pinnedMemAlloc_;
    size_t pinnedMemAllocSize_;

    /// Another option is to use a specified amount of memory on all
    /// devices
	/// 每个卡分配的临时显存池大小
    size_t tempMemSize_;

    /// Amount of pinned memory we should allocate
	/// pinned memory大小
    size_t pinnedMemSize_;

    /// Whether or not we log every GPU memory allocation and deallocation
	/// 是否有正在使用的显存（未被回收）
    bool allocLogging_;


/*
	上述中的temp memory主要用于算法的中间输出的临时储存
	上述中的pinned memory主要用于加速cpu和gpu之间的内存传输（具体实现还要仔细看看
*/
```

提供的函数接口按照功能分类包括一下几种：

- 初始化与全局设置
  - 初始化某张卡的所有资源（流、句柄、池）
  - 设置每张卡临时显存池大小
  - 设置pinned mem大小
  - 禁用临时显存池
  - 开启/关闭分配日志
- 流与句柄管理
  - 设置主流
  - 恢复为系统分配的主流
  - 获取主流
  - 获取备用流
  - 所有卡设置为null stream
  - 获取cuBLAS handle
  - 获取异步拷贝流
- 内存分配与管理
  - 分配显存（普通/链式，按需要走池化或者直接分配）
  - 释放现存
  - 查询当前临时池可用大小
  - 获取pinned mem指针和大小
  - 导出所有内存分配信息
- 析构与资源回收


#### MemorySpace

枚举类型数据，用于表示内存在GPU中的种类，定义如下：

```c++
/// Memory regions accessible to the GPU
enum MemorySpace {
    /// Temporary device memory (guaranteed to no longer be used upon exit of a
    /// top-level index call, and where the streams using it have completed GPU
    /// work). Typically backed by Device memory (cudaMalloc/cudaFree).
    Temporary = 0,      // 算法内部的临时缓冲区

    /// Managed using cudaMalloc/cudaFree (typical GPU device memory)
    Device = 1,         // 标准GPU设备内存，属于GPU显存

    /// Managed using cudaMallocManaged/cudaFree (typical Unified CPU/GPU
    /// memory)
    Unified = 2,        // CPU与GPU的共享同一内存，便于CPU/GPU协同计算
};
```

`AllocInfo`结构体用来表示与内存分配有关的信息，并方便调试与打印

```c++
/// Information on what/where an allocation is
struct AllocInfo {
    inline AllocInfo() {}

    inline AllocInfo(AllocType at, int dev, MemorySpace sp, cudaStream_t st)
            : type(at), device(dev), space(sp), stream(st) {}

    /// 方便打印调试
    std::string toString() const;

    /// 内存的使用方式，例如FlatData、IVFData
    AllocType type = AllocType::Other;

    /// 内存位于哪个设备
    int device = 0;

    /// 内存类型
    MemorySpace space = MemorySpace::Device;

    /// 该块内存与哪个cuda 流相关联
    cudaStream_t stream = nullptr;
};
````

## GpuIndexFlat的一些成员函数

`data_`作为真正储存数据的结构体，其类为`FlatIndex`

#### 构造函数

`GpuIndexFlat`的构造函数分为两种情况：

- 从CPU拷贝数据
  - 成员变量初始化
  - 调用自己的成员函数`copyFrom(cpuIndex)`从CPU拷贝数据
- 直接再GPU构造一个新的索引
  - 成员变量初始化
  - 调用自己的成员函数`resetIndex_(dims)`初始化

#### resetIndex_

重置GpuIndexFlat中实际储存索引的结构体`std::unique_ptr<FlatIndex> data_`，如果启用cuVS，则重置的结构体为`CuvsFlatIndex`，否则就是基类`FlatIndex`

#### copyFrom/copyTo

从CPU中的IndexFlat中拷贝数据到GPU中，通过`data_.add()`函数完成

#### getNumVecs

返回当前索引中记录的向量数

#### add

向索引中添加数据，传入参数为`idx_t n, const float* x`，具体操作为：

- 首先调用`data_->reserve`预先进行空间分配，避免后续添加拷贝数据时频繁的空间分配
- 如果是32位数据存储，调用`addImpl_`接口写入数据
  - `addImpl_`调用`data_->add`最终写入数据

#### reconstruct/\_n/\_batch

从数据中还原指定的一个/n个/一组数据

#### compute_residual_n

输入n个向量和n个key，计算第i个向量和第i个key对应的向量的残差数据，并返回

#### getGpuData

获取数据

#### searchImpl_

最终调用`data_->query`方法实现查询

***

> 面向骁龙GPU，需要重新实现的一些接口：
>
> - `FlatIndex` 类管理索引，包括添加、查询、重构、预分配、计算距离等接口
>   - 需要实现的子类
>   - `DeviceVector` 类作为一个STL来储存实际的vector数据
>     - `GpuMemoryReservation` 类作为实际使用GPU显存的结构体，内含`void* data`
>   - `DeviceTensor` 类作为一个张量视图来访问上述vector中的数据
>     - Kompute已经有tensor的实现
>   - 所有的类均支持float32和float16（可以暂时实现float32来实现基本功能
>   - 一些计算的辅助函数，如计算残差等函数需要重新实现
> - `GpuIndexFlat` 类，包括数据转移（CPU和GPU之间）、计算残差、查询等功能
>   - 需要实现的类
>   - `GpuIndex` 类，作为基类，提供**分页**、搜索、残差计算、数据添加等功能
>   - 通过一个`unique_ptr<FlatIndex>` 指向实际存储数据的地方
> - 这些类都会用到`GpuResources`，但是不需要实现，骁龙场景下只有一个GPU
>   - Kompute已经有manager的实现
> - 实现逻辑，成员函数接口均可以**直接参考原项目实现**，原项目有关GPU部分基本都需要nvcc编译，且使用了nvcc的一些编译修饰符，如`__host__`支持代码在GPU运行，`__device__`支持在CPU运行
> - 骁龙架构有无`cuda stream`类似的流的概念？需要额外考虑异步执行的情况





























